{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath' | { read -r f; case \"$f\" in *.ts|*.tsx|*.js|*.jsx|*.json) ./node_modules/.bin/prettier --write \"$f\" && ./node_modules/.bin/eslint --fix \"$f\" ;; esac; } 2>/dev/null || true",
            "statusMessage": "Formateando con Prettier y ESLint..."
          }
        ]
      }
    ]
  }
}
